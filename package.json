{
  "name": "samantha-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex-only Samantha control plane prototype",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test --path-ignore-patterns='worktrees/**' tests",
    "dispatch-worker": "bun run src/dispatch-worker.ts",
    "validate-fixture": "bun run src/index.ts validate-fixture"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^5.9.0"
  }
}
